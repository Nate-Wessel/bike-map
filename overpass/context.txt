// define a global bounding box (s,w,n,e)
[bbox:42.8,-80.8,45.0,-77.9];

// select ways and relations of interest from within the bounding box
(
	way[waterway];
	rel[waterway];
	way[railway];
	way[leisure=park];
	way[natural];
	rel[natural];
);

// join any children of the above ways and/or relations
(._;>;); 

out body;
